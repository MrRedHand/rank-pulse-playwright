import styles from './index.module.css'

export function SidebarFooter() {
  return (
    <footer className={styles.footer}>
      <p className={styles.footerText}>RankPulse / local</p>
    </footer>
  )
}
